<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wishmobile.tomazpractice">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoTitle">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tabview.TabViewActivty" />
        <activity android:name=".drawermenu.DrawerActivity" />
        <activity android:name=".pageindicator.CirclePageIndicatorActivity" />
        <activity android:name=".wallet.WalletActiviy" />
        <activity android:name=".wallet.ProfileActivity" />
        <activity android:name=".wallet.WalletPageActivity" />
        <activity android:name=".wallet.NewWalletActivity" />
        <activity android:name=".network.NetworkActivity"/>

        <service android:name="com.wishmobile.tomazpractice.network.QuestionApiService"/>


    </application>

</manifest>